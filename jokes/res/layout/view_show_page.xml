<com.ly.duan.view.SildingFinishLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/sfl"
    android:layout_width="match_parent"
    android:layout_height="match_parent" >

    <!-- 导航栏 -->
    <RelativeLayout
        android:id="@+id/rl"
        style="@style/NavStyle"
        android:layout_alignParentTop="true" >

        <TextView
            android:id="@+id/title"
            style="@style/NavCenterTVStyle"
            android:layout_centerInParent="true"
            android:ellipsize="end"
            android:maxWidth="200dip"
            android:singleLine="true"
            android:text="@string/app_name" />

        <ImageView
            android:id="@+id/back"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:contentDescription="@null"
            android:scaleType="fitXY"
            android:src="@drawable/back_selector" />
    </RelativeLayout>

    <LinearLayout
        android:id="@+id/ll_head"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/rl"
        android:layout_gravity="center_vertical"
        android:orientation="horizontal"
        android:visibility="gone" >

        <ImageView
            android:id="@+id/iv"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:contentDescription="@null"
            android:scaleType="fitXY" />

        <TextView
            android:id="@+id/tv"
            style="@style/PostNickStyle"
            android:layout_marginLeft="12dip" />
    </LinearLayout>

    <WebView
        android:id="@+id/webView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_alignParentBottom="true"
        android:layout_below="@id/ll_head" />

</com.ly.duan.view.SildingFinishLayout>